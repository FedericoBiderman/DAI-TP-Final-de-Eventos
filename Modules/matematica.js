const Pi= 3.1418;

const suma = (a,b) => a+b;


const multiplicar = (x, y) => 
{
 return x*y;
}

const dividir = (x,y) => {return x/y;}

const restar = (x,y)=>{return x-y;}

const strings = ["dos", "cuatro","ocho","diez"];

export{Pi, suma, restar, multiplicar, dividir, strings};